name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5

    - name: Run tests
      run: cargo test
